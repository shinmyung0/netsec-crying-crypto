#!/usr/bin/env bash

# this file is just a placeholder at the moment. But later on, I can create a bunch of those json
# config files and then queue up a bunch of commands to run results_retriver on them below.